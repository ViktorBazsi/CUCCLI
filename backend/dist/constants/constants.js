// export const PORT = Number(process.env.PORT) || 8080;
// export const HOST = process.env.HOST || "http://localhost";

export const {
  FRONTEND_URL
} = process.env;
export const {
  JWT_SECRET
} = process.env;